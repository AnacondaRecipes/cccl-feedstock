anaconda_config_version: 1
upstream_sources:
  - github:
      identifier: nvidia/cccl
      use_max_tag: true
      include_regex: ^v?\d+\.\d+\.\d+$
      exclude_regex: (?i)^(?=.*(?:\.dev(?:\d|$)|[-._]dev\d|rc\d|alpha|beta|preview)).+$
